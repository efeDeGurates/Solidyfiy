# Core
Flask==2.3.2
Flask-SQLAlchemy==3.0.3
Flask-Migrate==4.0.4
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.23  # Güncel sürüm
psycopg2-binary==2.9.9  # Güncel sürüm

# Supabase Integration
supabase==2.3.1  # EN ÖNEMLİ DEĞİŞİKLİK
gotrue==0.2.2
postgrest-py==0.10.7
storage-py==0.7.0  # StorageException için gerekli

# File Uploads & Processing
werkzeug==2.3.7  # Güncel sürüm
python-magic-bin==0.4.14; sys_platform == 'win32'  # Windows için
python-magic==0.4.27; sys_platform != 'win32'  # Diğer işletim sistemleri

# Security
itsdangerous==2.1.2
flask-admin==1.6.1

# Additional Utilities
uuid==1.30
python-dateutil==2.8.2
zipfile36==0.1.3

# Production
gunicorn==21.2.0  # Render için zorunlu